const modal=document.getElementById("bookingModal");const openers=document.querySelectorAll("[data-open-booking]");const close=document.querySelector("[data-close-booking]");const form=document.getElementById("bookingForm");const success=document.getElementById("success");function openModal(){modal.classList.add("show");modal.setAttribute("aria-hidden","false");document.body.style.overflow="hidden"}function closeModal(){modal.classList.remove("show");modal.setAttribute("aria-hidden","true");document.body.style.overflow=""}openers.forEach(b=>b.addEventListener("click",openModal));close.addEventListener("click",closeModal);modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});document.querySelectorAll(".slots button").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".slots button").forEach(x=>x.classList.remove("active"));b.classList.add("active")}));form.addEventListener("submit",e=>{e.preventDefault();success.textContent="✓ Appointment request received. Our care team will contact you shortly.";form.reset();document.querySelectorAll(".slots button").forEach(x=>x.classList.remove("active"))});const menu=document.querySelector(".menu"),nav=document.querySelector(".header nav");menu.addEventListener("click",()=>{nav.classList.toggle("mobile-open")});document.querySelectorAll(".header nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("mobile-open")));const citySearch=document.getElementById("citySearch");citySearch.addEventListener("input",e=>{const q=e.target.value.toLowerCase();document.querySelectorAll(".cities button").forEach(b=>b.style.display=b.textContent.toLowerCase().includes(q)?"block":"none")});